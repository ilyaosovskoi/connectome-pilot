# Full FlyWire connectome — fetched by script, not for git (~127 MB).
# python3 scripts/download_full.py
data/*.parquet
data/*.tsv
data/*.part
# Full-brain runtime NPZ (~300 MB, local experiment artifact).
data/fly_full.npz

# Local agent reports (session context, not for push).
# See notes/HANDOFF.md.
notes/

.DS_Store
__pycache__/
*.pyc
